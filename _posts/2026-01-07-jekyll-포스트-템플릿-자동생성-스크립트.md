---
title: "jekyll 포스트 템플릿 자동생성 스크립트"
date: 2026-01-07
categories: [github]
tags: [github pages, jekyll, script, post]
---

github pages 블로그 포스트 새글 작성시 스크립트를 통해 기본 템플릿을 쉽고 빠르게 생성하도록 한다.
---

## new-post.sh 파일 생성
```
# 스크립트 파일 생성
touch ./scripts/new-post.sh
```

## 스크립트 내용 작성
```
#!/bin/bash

# post 기본 템플릿 파일 생성을 위한 자동 스크립트 입니다.
# root 경로에서 sh ./scripts/new-post.sh "포스트 제목" 명령어를 입력하여 사용

TITLE="$1"
DATE=$(date +%Y-%m-%d)
SLUG=$(echo "$TITLE" | tr '[:upper:]' '[:lower:]' | sed 's/ /-/g')

# FILE="_posts/${DATE}-${SLUG}.md" # 특정 경로 지정
FILE="./${DATE}-${SLUG}.md" # 현재 위치에 파일 생성

cat <<EOF > $FILE
---
title: "$TITLE"
date: $DATE
categories: []
tags: []
---

EOF

echo "Created: $FILE"

```
